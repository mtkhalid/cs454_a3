/*
	Bismillah
*/	
#include <iostream>
#include <map>
using namespace std;

int main(){
	map<string, string> binderDB;
	//binderDB.insert( pair<char, int>('a', 100) );
	binderDB.insert( pair<string, string>(procSignature, serverLoc) );			//this procedure is located at this server location
}
